# Домашнее задание к занятию «Защита сети»

### Подготовка к выполнению заданий

1. Подготовка защищаемой системы:

- установите **Suricata**,
- установите **Fail2Ban**.

2. Подготовка системы злоумышленника: установите **nmap** и **thc-hydra** либо скачайте и установите **Kali linux**.

Обе системы должны находится в одной подсети.

------

### Задание 1

Проведите разведку системы и определите, какие сетевые службы запущены на защищаемой системе:

**sudo nmap -sA < ip-адрес >**

**sudo nmap -sT < ip-адрес >**

**sudo nmap -sS < ip-адрес >**

**sudo nmap -sV < ip-адрес >**

По желанию можете поэкспериментировать с опциями: https://nmap.org/man/ru/man-briefoptions.html.


*В качестве ответа пришлите события, которые попали в логи Suricata и Fail2Ban, прокомментируйте результат.*

### Решение 1  

![alt text](https://github.com/fin4457/13-03/blob/main/img/1.jpg)
![alt text](https://github.com/fin4457/13-03/blob/main/img/2.jpg)
![alt text](https://github.com/fin4457/13-03/blob/main/img/3.jpg)

1. События в логах Suricata.
   Suricata зафиксировала множественные попытки сканирования портов популярных СУБД и сервисов удаленного доступа. Подозрительный трафик исходил от хоста 192.168.44.130. Основные зафиксированные типы сигнатур:ET SCAN Suspicious inbound to mySQL port 3006 / 3306 — попытки сканирования или подключения к СУБД MySQL. ET SCAN Suspicious inbound to MSSQL port 1433 — поиск открытых портов СУБД Microsoft SQL Server. ET SCAN Suspicious inbound to PostgreSQL port 5432 — сканирование портов СУБД PostgreSQL. ET SCAN Suspicious inbound to Oracle SQL port 1521 — сканирование портов СУБД Oracle. ET SCAN Potential VNC Scan 5800-5820 — попытки обнаружения сервиса удаленного рабочего стола VNC.
2. События в логах Fail2Ban.
   Файл логов по пути /var/log/fail2ban.log на целевой машине Ubuntu оказался абсолютно пустым. Проводилось сканирование хоста 192.168.44.131 с помощью утилиты Nmap (использовались флаги -sA, -sT, -sS, -sV). Результаты сканирования Nmap показывают, что на целевой машине открыт только один порт — 22/tcp (SSH), на котором работает служба OpenSSH 6.6p1. Все остальные 999 стандартных портов (включая СУБД, такие как MySQL или PostgreSQL) закрыты (closed) или фильтруются. Сканирование Nmap было направлено на поиск портов баз данных, которые на целевой машине закрыты. Следовательно, никаких попыток авторизации в СУБД не происходило. Даже если бы порты были открыты, Fail2Ban по умолчанию защищает сервисы от подбора паролей (Brute-force) на основе анализа логов аутентификации, а не от самого факта сетевого сканирования. Поскольку подбора паролей не было к SSH, Fail2Ban не зафиксировал аномалий.
------

### Задание 2

Проведите атаку на подбор пароля для службы SSH:

**hydra -L users.txt -P pass.txt < ip-адрес > ssh**

1. Настройка **hydra**: 
 
 - создайте два файла: **users.txt** и **pass.txt**;
 - в каждой строчке первого файла должны быть имена пользователей, второго — пароли. В нашем случае это могут быть случайные строки, но ради эксперимента можете добавить имя и пароль существующего пользователя.

Дополнительная информация по **hydra**: https://kali.tools/?p=1847.

2. Включение защиты SSH для Fail2Ban:

-  открыть файл /etc/fail2ban/jail.conf,
-  найти секцию **ssh**,
-  установить **enabled**  в **true**.

Дополнительная информация по **Fail2Ban**:https://putty.org.ru/articles/fail2ban-ssh.html.



*В качестве ответа пришлите события, которые попали в логи Suricata и Fail2Ban, прокомментируйте результат.*
